# 马丁窗口表探查结果

探查时间：2026-07-16

## 结论

- 表：`risk.dws_account_martingale_window`
- 字段数：28
- `window_type` 有 `7D_SLIDING` 和 `CUSTOM` 两种；快照默认使用 `7D_SLIDING`。
- `7D_SLIDING` 覆盖 `2026-05-09` 至 `2026-07-16`，共 248,547 行。
- `CUSTOM` 覆盖 `2025-01-28` 至 `2026-07-16`，共 810 行。
- `avg_volume_escalation` 的注释为“序列最后一笔/第一笔交易量的平均放大比例”，且实际分布以 1.0 附近为中心，因此口径是倍数，不是增长率；`1.5` 表示放大 50%。
- 快照按窗口与选择期相交的规则取数：`window_end >= selection_start AND window_start < selection_end + 1 day`。

## escalation 分桶

| 区间 | 行数 |
|---|---:|
| `<1.0` | 50,116 |
| `1.0–1.1` | 123,477 |
| `1.1–1.2` | 12,336 |
| `1.2–1.3` | 9,401 |
| `1.3–1.5` | 10,529 |
| `1.5–1.8` | 11,757 |
| `>=1.8` | 31,741 |

完整 schema、覆盖范围和原始分桶结果保存在同目录的 `2026-07-16-martingale-schema.json`。
