# R4 因子迁移说明（给人 / AI 阅读）

> 目的：让任何人或 AI，仅凭 `risk.dws_account_holding_buckets_window`（或本地导出的同结构 CSV），就能**复现 R4 筛选逻辑**，产出 5–6 月（或任意日期）的候选用户名单。  
> 研究阈值保留在本文作为审计基线；生产运行阈值由应用配置管理，不在当前 5–6 月样本上重新计算分位点。不要用总利润 Top 当筛选条件。

---

## 0. 一句话定义

**R4 = 风格与优势对齐 + 交易时长集中 + 整体胜率够高**

研究基线规则：

```text
soft_alignment == 1
AND primary_trade_pct >= 0.381
AND win_rate >= 0.589
AND total_trades >= 10
AND primary_trades >= 5
```

当前生产运行规则（用于 Abook 路由，降低精确分位点过拟合风险）：

```text
soft_alignment == 1
AND primary_trade_pct >= 0.35
AND win_rate >= 0.55
AND total_trades >= 20
AND primary_trades >= 8
```

更强版（推荐做重点名单）：

```text
本周满足 R4
AND 上一非重叠周（约前 7 天）也满足 R4
```

---

## 1. 数据源

| 项目 | 值 |
|------|-----|
| ClickHouse 表 | `risk.dws_account_holding_buckets_window` |
| 本地样例/导出 | `/Users/jianghe/holding_buckets/account_holding_buckets.csv` |
| **只用** | `window_type = '7D_SLIDING'` |
| **不要用** | `CUSTOM` 长窗（研究与筛选均未采用） |

### 1.1 表字段（筛选需要用到的）

| 字段 | 类型含义 | 用途 |
|------|----------|------|
| `platform` | 平台（mt4/mt5/...） | 用户键 |
| `login` | 账号 | 用户键 |
| `window_type` | 窗口类型 | 过滤 = `7D_SLIDING` |
| `window_start` | 7 日窗开始 | 可选 |
| `window_end` | 7 日窗结束 | **特征时点**；筛 5–6 月用此字段 |
| `bucket` | 持仓时长桶 | 主战场 / Top2 / 最赚钱桶 |
| `trade_count` | 该桶交易笔数 | 聚合、门槛 |
| `trade_pct` | 该桶交易占比 | 集中度、Top2 |
| `winning_count` | 该桶盈利笔数 | 算整体 win_rate |
| `win_rate` | 该桶胜率 | 仅桶级；整体需重算 |
| `total_profit` | 该桶总利润 | 找最赚钱桶 |
| `avg_profit` | 该桶均笔利润 | 可选（R5） |
| `computed_at` | 计算时间 | 可选 |

### 1.2 数据粒度（关键）

**一行 = 一个用户 × 一个 7 日窗口 × 一个 bucket。**

一个用户在同一 `window_end` 下通常有多行（最多约 11 个 bucket）。  
R4 需要的是 **用户 × 窗口** 一行特征，必须先聚合。

### 1.3 Bucket 取值（固定集合）

```text
0_10s, 10_30s, 30_60s, 1_2min, 2_5min, 5_15min,
15_30min, 30_60min, 1_4h, 4_24h, 24h_plus
```

---

## 2. 从原始行构建 R4 所需特征

对每个 `(platform, login, window_end)` 的所有 bucket 行，计算：

### 2.1 整体胜率 `win_rate`

```text
total_trades  = sum(trade_count)
total_wins    = sum(winning_count)
win_rate      = total_wins / total_trades     # total_trades > 0
```

**注意：** 不能对桶级 `win_rate` 做简单平均；必须用笔数加权（即上式）。

### 2.2 主战场集中度 `primary_trade_pct`

```text
primary_bucket     = argmax(trade_pct)
                     # 若并列，取 trade_count 更大的 bucket
primary_trade_pct  = 该桶的 trade_pct
primary_trades     = 该桶的 trade_count
```

### 2.3 软对齐 `soft_alignment`

1. **交易量 Top2 桶**  
   按 `(trade_pct DESC, trade_count DESC)` 排序，取前 2 个 `bucket`，记为集合 `Top2`。

2. **最赚钱桶 `best_profit_bucket`**  
   仅在 `trade_count >= 3` 的桶中，取 `total_profit` 最大的 `bucket`  
   （并列取 `trade_count` 更大者）。  
   若没有满足 `trade_count >= 3` 的桶 → `soft_alignment = 0`。

3. **对齐判定**

```text
soft_alignment = 1  当且仅当  best_profit_bucket ∈ Top2
soft_alignment = 0  否则
```

含义：最赚钱的持仓时长，是否落在用户交易最多的两个时长里（风格与优势对齐）。

### 2.4 样本门槛（必须）

```text
total_trades   >= 10
primary_trades >= 5
```

不满足则**不进入** R4 候选（噪声太大）。

### 2.5 （可选）R5 附加

```text
avg_profit = sum(total_profit) / total_trades
R5 = R4 AND avg_profit > 0
```

---

## 3. R4 判定（生产阈值固定，勿重算当前样本分位数）

```text
pass_R4 =
  soft_alignment == 1
  AND primary_trade_pct >= 0.35
  AND win_rate >= 0.55
  AND total_trades >= 20
  AND primary_trades >= 8
```

| 阈值 | 值 | 来源 |
|------|-----|------|
| `win_rate` | `>= 0.55` | 生产运行阈值；提高样本外稳健性 |
| `primary_trade_pct` | `>= 0.35` | 生产运行阈值；避免精确分位点脆弱性 |
| `total_trades` | `>= 20` | 生产运行阈值；提高胜率估计样本量 |
| `primary_trades` | `>= 8` | 生产运行阈值；避免主战场占比由少数交易决定 |
| `soft_alignment` | `== 1` | 规则条件 |

研究阶段的 `38.1% / 58.9% / 10 / 5` 保留为审计基线，不再作为生产路由阈值。**禁止：** 用当前要筛选的 5–6 月数据重新算分位数再当门槛（会乐观偏倚 / 未来函数式偷看）。

---

## 4. 如何用 5–6 月数据筛用户

### 4.1 时间过滤

筛「2026 年 5–6 月」时：

```text
window_type = '7D_SLIDING'
AND window_end >= '2026-05-01 00:00:00'
AND window_end <  '2026-07-01 00:00:00'
```

说明：`7D_SLIDING` 按天滚动，同一用户在 5–6 月会有很多个 `window_end`。

### 4.2 去重（强烈建议）

日滑窗高度重叠。业务名单请做**周快照**：

```text
feat_week = 以 window_end 所在周（建议 W-SUN 或 ISO week）
每个 (platform, login, feat_week) 只保留该周最晚的 window_end
```

再在该快照上算 `pass_R4`。

### 4.3 两种输出方式

**A. 单周 R4 名单**  
某一周（或 5–6 月每周）`pass_R4 = 1` 的 `(platform, login)`。

**B. 连续两周 R4（更强，推荐重点关注）**

```text
本周 pass_R4 = 1
AND 上一非重叠周 pass_R4 = 1
```

「非重叠周」：上一快照的 `window_end` 至少比本周 `window_end` 早 7 天（或直接取上周周快照）。

研究中：单周 R4 覆盖约 8–9%，下周盈利概率约 54–58%；连续两周更强、人数更少。

### 4.4 输出字段建议

最少：

```text
platform, login, window_end, feat_week,
win_rate, primary_bucket, primary_trade_pct, primary_trades,
best_profit_bucket, soft_alignment, total_trades,
pass_R4
```

可选：`avg_profit`, `pass_R5`, `pass_R4_two_weeks`。

---

## 5. 可直接迁移的 ClickHouse SQL（骨架）

```sql
-- R4 单周筛选：5–6 月示例
WITH base AS (
  SELECT
    platform,
    login,
    window_end,
    bucket,
    trade_count,
    trade_pct,
    winning_count,
    total_profit
  FROM risk.dws_account_holding_buckets_window
  WHERE window_type = '7D_SLIDING'
    AND window_end >= toDateTime('2026-05-01 00:00:00')
    AND window_end <  toDateTime('2026-07-01 00:00:00')
),
agg AS (
  SELECT
    platform, login, window_end,
    sum(trade_count) AS total_trades,
    sum(winning_count) AS total_wins,
    sum(total_profit) AS window_total_profit,
    total_wins / nullIf(total_trades, 0) AS win_rate
  FROM base
  GROUP BY platform, login, window_end
),
primary_b AS (
  SELECT
    platform, login, window_end,
    argMax(bucket, (trade_pct, trade_count)) AS primary_bucket,
    max(trade_pct) AS primary_trade_pct,
    argMax(trade_count, (trade_pct, trade_count)) AS primary_trades
  FROM base
  GROUP BY platform, login, window_end
),
best_b AS (
  SELECT
    platform, login, window_end,
    argMax(bucket, (total_profit, trade_count)) AS best_profit_bucket
  FROM base
  WHERE trade_count >= 3
  GROUP BY platform, login, window_end
),
top2 AS (
  SELECT
    platform, login, window_end,
    groupArray(bucket) AS top2_buckets
  FROM (
    SELECT
      platform, login, window_end, bucket,
      row_number() OVER (
        PARTITION BY platform, login, window_end
        ORDER BY trade_pct DESC, trade_count DESC
      ) AS rn
    FROM base
  )
  WHERE rn <= 2
  GROUP BY platform, login, window_end
),
feat AS (
  SELECT
    a.platform,
    a.login,
    a.window_end,
    toStartOfWeek(a.window_end, 0) AS feat_week,  -- 按需调整周起点
    a.total_trades,
    a.win_rate,
    a.window_total_profit / nullIf(a.total_trades, 0) AS avg_profit,
    p.primary_bucket,
    p.primary_trade_pct,
    p.primary_trades,
    b.best_profit_bucket,
    t.top2_buckets,
    if(b.best_profit_bucket = '' OR b.best_profit_bucket IS NULL, 0,
       has(t.top2_buckets, b.best_profit_bucket)) AS soft_alignment
  FROM agg a
  INNER JOIN primary_b p USING (platform, login, window_end)
  LEFT JOIN best_b b USING (platform, login, window_end)
  INNER JOIN top2 t USING (platform, login, window_end)
),
-- 每周只留最晚 window_end
weekly AS (
  SELECT *
  FROM feat
  WHERE (platform, login, feat_week, window_end) IN (
    SELECT platform, login, feat_week, max(window_end)
    FROM feat
    GROUP BY platform, login, feat_week
  )
)
SELECT
  platform,
  login,
  window_end,
  feat_week,
  win_rate,
  primary_bucket,
  primary_trade_pct,
  primary_trades,
  best_profit_bucket,
  soft_alignment,
  total_trades,
  avg_profit,
  1 AS pass_R4
FROM weekly
WHERE total_trades >= 10
  AND primary_trades >= 5
  AND soft_alignment = 1
  AND primary_trade_pct >= 0.381
  AND win_rate >= 0.589
ORDER BY window_end, platform, login;
```

本地 CSV 路径：`/Users/jianghe/holding_buckets/account_holding_buckets.csv`  
列名与上表一致（带引号的 CSV header）。逻辑相同，用 pandas / SQL on file 实现即可。

---

## 6. Python 伪代码（便于 AI 实现）

```python
# 输入：DataFrame rows of 7D_SLIDING in May–Jun
# 输出：weekly R4 users

# 1) filter
df = df[df.window_type == "7D_SLIDING"]
df = df[(df.window_end >= "2026-05-01") & (df.window_end < "2026-07-01")]

# 2) group key = (platform, login, window_end)
#    win_rate = sum(winning_count)/sum(trade_count)
#    primary = row with max trade_pct (tie: trade_count)
#    top2 = top 2 by trade_pct
#    best = among trade_count>=3, max total_profit
#    soft_alignment = best.bucket in top2.buckets

# 3) weekly snapshot: per (platform,login,week) keep latest window_end

# 4) pass_R4 = (
#      soft_alignment==1
#      and primary_trade_pct >= 0.381
#      and win_rate >= 0.589
#      and total_trades >= 10
#      and primary_trades >= 5
#    )
```

---

## 7. AI 执行检查清单

实现时请逐项确认：

1. [ ] 只用 `7D_SLIDING`，未混入 `CUSTOM`
2. [ ] `win_rate` 用 `sum(winning)/sum(trades)`，不是桶 win_rate 均值
3. [ ] `primary_trade_pct` 来自 max `trade_pct` 的桶
4. [ ] `best_profit_bucket` 仅在 `trade_count >= 3` 中选取
5. [ ] `soft_alignment` = best ∈ Top2（按 trade_pct）
6. [ ] 阈值固定为 `0.589` / `0.381`，未在筛选样本上重算分位
7. [ ] 有 `total_trades>=10` 且 `primary_trades>=5`
8. [ ] 5–6 月用 `window_end` 过滤；周快照去重
9. [ ] 未用「当期总利润 TopN」作为筛选条件（该信号均值回归，研究已证伪）
10. [ ] 若要更强名单，输出「连续两周 R4」

---

## 8. 研究结论摘要（为何迁移 R4）

- 后续非线性/单调性/策略指纹研究（V2/V3）**没有找到显著强于 R4 的新主规则**。
- R4 在无未来函数、时间切分、bootstrap / 滚动验证下稳定有效。
- 「持仓越长胜率越高」的漂亮曲线**不是**好筛选信号；R4 抓的是**对齐 + 专注 + 高胜率**。

相关本地研究产出（可选参考，迁移不依赖）：

- `/Users/jianghe/holding_buckets/research/研究说明.md`
- `/Users/jianghe/holding_buckets/research/SUMMARY.md`
- `/Users/jianghe/holding_buckets/research/weekly_meta.json`（阈值来源）

---

## 9. 常见错误

| 错误 | 后果 |
|------|------|
| 用 `CUSTOM` 窗口 | 与研究口径不一致 |
| 桶 win_rate 简单平均 | 整体胜率算错 |
| 在 5–6 月数据上重算 p70 | 阈值泄漏，结果虚高 |
| 用总利润 / avg_profit Top 筛人 | 均值回归，下周更差 |
| 把每天滑窗全当独立样本输出名单 | 同一用户重复刷屏 |
| 把 R4 当「永久高手」标签 | R4 是状态，需每周重算 |

---

## 10. 最小验收

跑完后应能回答：

1. 5–6 月每周有多少用户过 R4？  
2. 其中多少人连续两周过 R4？  
3. 抽查 1 个 login：能否从原始 bucket 行手算复现 `win_rate`、`primary_trade_pct`、`soft_alignment`？

若第 3 点手算不一致，实现有 bug，不要上线。
