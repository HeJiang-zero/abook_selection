<script setup lang="ts">
import type { AnalysisPayload } from '../types'
defineProps<{ data: AnalysisPayload }>()
</script>
<template>
  <section class="two-col"><article class="panel"><div class="panel-head"><h2>Abook 误判</h2><span class="negative">公司少赚 {{ (data.misjudge?.abook_loss_total ?? 0).toFixed(2) }}</span></div><div v-if="!data.misjudge?.abook_losses?.length" class="empty">暂无 Abook 亏损用户</div><div v-for="row in data.misjudge?.abook_losses || []" :key="row.login" class="list-row"><span>{{ row.platform }} / {{ row.login }}<small>客户净 P&amp;L {{ row.validation_client_net_pnl.toFixed(2) }} · 若改为 Abook 公司增量 {{ row.company_increment_if_routed.toFixed(2) }}</small></span><b class="negative">{{ row.company_increment_if_routed.toFixed(2) }}</b></div></article><article class="panel"><div class="panel-head"><h2>Bbook 漏网</h2><span class="negative">公司损失 {{ (data.misjudge?.bbook_company_loss_total ?? 0).toFixed(2) }}</span></div><div v-for="row in data.misjudge?.bbook_profitable || []" :key="row.login" class="list-row"><span>{{ row.platform }} / {{ row.login }}<small>客户净 P&amp;L {{ row.validation_client_net_pnl.toFixed(2) }} · 留在 Bbook 公司影响 {{ row.company_loss_if_left_bbook.toFixed(2) }}</small></span><b class="negative">{{ row.company_loss_if_left_bbook.toFixed(2) }}</b></div><div v-if="!data.misjudge?.bbook_profitable?.length" class="empty">暂无漏网用户</div></article></section>
</template>
